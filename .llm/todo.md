# Change App Name from Udhetim to Hitch

> Generated by /plan for: change the app name to hitch
> Codebase: udhetim (Next.js ridesharing app)
> Date: 2026-01-04

## Tasks

- [x] Update appName in English translations (src/messages/en.json)
- [x] Update appName in Albanian translations (src/messages/sq.json)
- [x] Update metadata title in layout (src/app/[locale]/layout.tsx)
- [x] Update package.json project name
- [x] Update README.md with new app name
- [x] Update PRD.md references to app name

## Context for Execution

**Tech Stack:** Next.js 16, TypeScript, Tailwind CSS, next-intl for i18n

**Key Files:**
- `src/messages/en.json` - English translations, line 3 has appName
- `src/messages/sq.json` - Albanian translations, line 3 has appName (NOTE: "udhetim" is also an Albanian word for "journey" - only change the appName field, not other occurrences)
- `src/app/[locale]/layout.tsx` - Contains page title metadata "Udhetim - Albanian Ridesharing"
- `src/components/layout/Header.tsx` - Header component uses t("common.appName") so it auto-updates
- `package.json` - Project name field

**Notes:**
- The Albanian word "udhetim" means "journey/travel" - it appears throughout sq.json as actual Albanian text, NOT as the app name. Only change `common.appName`, not other occurrences.
- The Header component uses `t("common.appName")` so it will auto-update when translations change
- Update metadata title from "Udhetim - Albanian Ridesharing" to "Hitch - Albanian Ridesharing"

## Summary

- **Tasks:** 6 items
- **Estimated complexity:** Low
- **Key areas affected:** i18n translations, layout metadata, package config, documentation

Ready to execute with `/todo-all`
